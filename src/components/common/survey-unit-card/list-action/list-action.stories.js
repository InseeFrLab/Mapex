import React from 'react';
import ListAction from './component';

export default {
	title: 'Components/Section/ListAction',
	component: ListAction,
};

const Template = (args) => <ListAction {...args} />;

export const Default = Template.bind({});

Default.args = {};

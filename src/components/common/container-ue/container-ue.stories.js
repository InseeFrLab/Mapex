import React from 'react';
import ContainerUE from './component';

export default {
	title: 'Components/ContainerUE',
	component: ContainerUE,
};

const Template = (args) => <ContainerUE {...args} />;

export const Default = Template.bind({});
Default.args = {};
